ansible@ansible:~ $ python3 -m venv ~/ansible_venv
ansible@ansible:~ $ source ~/ansible_venv/bin/activate

# Create the main Ansible project directory (if not already done)
mkdir -p ~/ansible_home_guardian

# Create the directory to hold the Docker files (Dockerfile, docker-compose.yml)
mkdir -p ~/home-network-guardian-files

(ansible_venv) ansible@ansible:~ $ ls -l
total 16
drwxrwxr-x 2 ansible ansible 4096 Oct 27 20:39 ansible_home_guardian
drwxrwxr-x 5 ansible ansible 4096 Oct 27 20:32 ansible_venv
drwxrwxr-x 2 ansible ansible 4096 Oct 27 20:39 home-network-guardian-files
-rw-rw-r-- 1 ansible ansible  241 Oct 27 20:10 hosts.ini
(ansible_venv) ansible@ansible:~ $ nano hosts.ini
